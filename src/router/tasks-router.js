const express = require('express');

const {} = require('../controllers/tasks-controller.js');

const Router = express.Router();

// Router.route('/id').get().post().delete().update();

module.exports = Router;
